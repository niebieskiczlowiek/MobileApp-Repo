import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    sprite: {
      width: 200,
      height: 200
    },
    sprite_container: {
      alignItems: 'center',
      paddingTop: 30
    }
  });

export default styles